---
import HTML from "./HTML.astro";
import ModalLoader from "../modals/ModalLoader.svelte";
import HeaderAndHero from "./HeaderAndHero.svelte";
import Footer from "./footer/Footer.svelte";

const deafaultModalIds = ["termsAndConditions", "contact", "auth"]

const { 
    title,
    modalIds = []
 } = Astro.props;

 const ids = [...deafaultModalIds, ...modalIds]

---

<HTML title={title}>
	<ModalLoader modalIds={ids} client:load />
	<HeaderAndHero client:load>
		<slot name="hero" />
	</HeaderAndHero>

	<main>
		<slot />
	</main>

	<Footer client:load />
</HTML>
